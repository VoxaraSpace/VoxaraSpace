# Documentation

The bot and protocol documentation is published at
https://voxaraspace.com/developers/docs/ and kept in step with the SDK in
`../sdk/`. The SDK file itself starts with a worked example; `../examples/`
has a complete bot using the SDK and one using the raw protocol.
